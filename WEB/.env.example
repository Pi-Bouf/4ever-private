# DB connection for the extract script (Prisma + @prisma/adapter-mssql).
# The SA password is read from the repo-root ../.env (SA_PASSWORD) — do not put it here.
# These optional overrides default to the docker MSSQL container.
DB_HOST=127.0.0.1
DB_PORT=11433
DB_NAME=TGame_gsp
DB_USER=sa
